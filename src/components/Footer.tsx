import { Mail, Github, Linkedin, Twitter } from 'lucide-react';
import { Link } from 'react-router-dom';

export const Footer = () => {
  return (
    <footer className="p-6 text-white font-mono mt-auto">
      <div className="space-y-4 text-xs">
        {/* Footer content removed */}
      </div>
    </footer>
  );
};
